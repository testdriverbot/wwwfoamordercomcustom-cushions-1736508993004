version: 4.1.35
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
  - prompt: Scroll to the bottom of the page if necessary.
    commands:
      - command: scroll
        direction: down
